#include "mat.h"
#include <stdio.h>

bool scatter_light(t_ray in, struct s_hit_record rec, t_vec3 *attenuation, t_ray *scattered)
{
	(void)in;
	(void)rec;
	(void)attenuation;
	(void)scattered;
	return FALSE;
}

